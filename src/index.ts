console.log('Hello, TypeScript!');

// Example of TypeScript features
interface User {
  name: string;
  age: number;
}

const user: User = {
  name: 'John Doe',
  age: 30
};

console.log(`User: ${user.name}, Age: ${user.age}`); 